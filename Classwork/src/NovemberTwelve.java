
public class NovemberTwelve {

	/**
	 * Node consist of 
	 * an item/object/value
	 * a left node
	 * a right node
	 * 
	 * 
	 * 
	 * A tree which does not have any node other than root node is called a null tree
	 * 
	 * 
	 * A leaf node has no children (points to two nulls)
	 * 
	 * The depth/height of a tree is the length of the path from the ROOT to the deeoest node in the tree.
	 * 
	 * a full binary tree - if each node is either a LEAF or posseses exactly two child Nodes
	 * 
	 * complete binary tree - a tree with n levels is complete if all levels except possibly the last are completely full, and has all its node 
	 * to the left side
	 * 
	 * A binary tree in which every level except possibly the deepest
	 * is completely filled. At depth n, all nodes must be as far left as possible
	 * 
	 * HOMEWORK - draw a complete binary tree
	 */
	public static void main(String[] args) {
		// TODO Auto-generated method stub

	}

}
